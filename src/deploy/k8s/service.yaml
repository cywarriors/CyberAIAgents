apiVersion: v1
kind: Service
metadata:
  name: threat-detection-agent
  labels:
    app: threat-detection-agent
spec:
  selector:
    app: threat-detection-agent
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  type: ClusterIP
