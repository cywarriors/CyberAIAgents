apiVersion: v1
kind: Namespace
metadata:
  name: vulnerability-mgmt

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vulnerability-mgmt-api
  namespace: vulnerability-mgmt
  labels:
    app: vulnerability-mgmt-api
    version: v1
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: vulnerability-mgmt-api
  template:
    metadata:
      labels:
        app: vulnerability-mgmt-api
        version: v1
    spec:
      serviceAccountName: vulnerability-mgmt-api
      containers:
      - name: vulnerability-mgmt-api
        image: vulnerability-mgmt-agent:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: api
          containerPort: 8003
          protocol: TCP
        - name: metrics
          containerPort: 8004
          protocol: TCP
        env:
        - name: VM_AGENT_API_HOST
          value: "0.0.0.0"
        - name: VM_AGENT_API_PORT
          value: "8003"
        - name: LOG_LEVEL
          value: "info"
        - name: ENABLE_METRICS
          value: "true"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: vulnerability-mgmt-secrets
              key: database-url
        resources:
          requests:
            cpu: 500m
            memory: 512Mi
          limits:
            cpu: 2000m
            memory: 2Gi
        livenessProbe:
          httpGet:
            path: /api/v1/admin/health
            port: api
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/v1/admin/health
            port: api
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 2
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          seccompProfile:
            type: RuntimeDefault
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir:
          sizeLimit: 100Mi

---
apiVersion: v1
kind: Service
metadata:
  name: vulnerability-mgmt-api
  namespace: vulnerability-mgmt
  labels:
    app: vulnerability-mgmt-api
spec:
  type: ClusterIP
  ports:
  - name: api
    port: 80
    targetPort: 8003
    protocol: TCP
  - name: metrics
    port: 8004
    targetPort: 8004
    protocol: TCP
  selector:
    app: vulnerability-mgmt-api

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: vulnerability-mgmt-api-hpa
  namespace: vulnerability-mgmt
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: vulnerability-mgmt-api
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vulnerability-mgmt-api
  namespace: vulnerability-mgmt

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: vulnerability-mgmt-api
  namespace: vulnerability-mgmt
rules:
- apiGroups: [""]
  resources: ["configmaps"]
  resourceNames: ["vulnerability-mgmt-config"]
  verbs: ["get", "watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: vulnerability-mgmt-api
  namespace: vulnerability-mgmt
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: vulnerability-mgmt-api
subjects:
- kind: ServiceAccount
  name: vulnerability-mgmt-api
  namespace: vulnerability-mgmt
