apiVersion: apps/v1
kind: Deployment
metadata:
  name: incident-triage-agent
  labels:
    app: incident-triage-agent
    srs: SRS-CYBER-02
spec:
  replicas: 3
  selector:
    matchLabels:
      app: incident-triage-agent
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: incident-triage-agent
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: incident-triage-agent
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: agent
          image: incident-triage-agent:latest
          ports:
            - containerPort: 8081
              name: http
          envFrom:
            - configMapRef:
                name: incident-triage-agent-config
            - secretRef:
                name: incident-triage-agent-secrets
          resources:
            requests:
              cpu: "2"
              memory: 8Gi
            limits:
              cpu: "8"
              memory: 32Gi
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
