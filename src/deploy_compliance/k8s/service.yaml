apiVersion: v1
kind: Service
metadata:
  name: compliance-audit-agent
spec:
  selector: {app: compliance-audit-agent}
  type: ClusterIP
  ports:
    - {name: api, port: 8010, targetPort: 8010}
    - {name: health, port: 8090, targetPort: 8090}
    - {name: metrics, port: 9100, targetPort: 9100}
