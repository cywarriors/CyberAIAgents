apiVersion: v1
kind: Service
metadata:
  name: vapt-agent
  labels:
    app: vapt-agent
    srs: SRS-CYBER-13
spec:
  selector:
    app: vapt-agent
  ports:
    - name: health
      port: 8083
      targetPort: 8083
    - name: metrics
      port: 9091
      targetPort: 9091
  type: ClusterIP
